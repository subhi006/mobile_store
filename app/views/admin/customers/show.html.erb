<div style="display: inline;">
  <%= tag.p "Name: #{@customer.first_name}" %>
</div>

<div>
  <%= tag.p "Number: #{@customer.number}" %>
  <%= tag.p "Password: #{@customer.password}" %>
</div>

<div class="product-actions">
  <%# <%= link_to "Edit Product", edit_admin_product_path(@customer), class: "btn btn-primary" %> 
  <%= button_to "Delete", admin_customer_path(@customer), method: :delete, 
      class: "btn btn-danger", 
      data: { turbo_confirm: "Are you sure?" } %>
</div>
